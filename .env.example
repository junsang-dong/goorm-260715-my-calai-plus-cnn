# Server-side only — never prefix with VITE_
# 아래 값을 실제 OpenAI API 키로 교체하세요.
OPENAI_API_KEY=sk-your-openai-api-key

# Optional: gpt-4o (default) | gpt-4.1 | gpt-5.2 등 Vision 지원 모델
# OPENAI_VISION_MODEL=gpt-4o
